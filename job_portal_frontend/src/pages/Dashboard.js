import React from "react";

/**
 * PUBLIC_INTERFACE
 * Dashboard page for user (employer/candidate).
 */
export default function Dashboard() {
  return (
    <div>
      <h2>Dashboard</h2>
      <p>Your dashboard (applications, management, etc.) will display here.</p>
    </div>
  );
}
